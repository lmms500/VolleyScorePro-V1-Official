# VolleyScore Pro Keystore Configuration
# =========================================
# Copy this file to 'keystore.properties' and fill in your values
# NEVER commit the actual keystore.properties file to version control!
#
# To generate a new keystore, run:
# keytool -genkey -v -keystore release-key.jks -alias volleyscore -keyalg RSA -keysize 2048 -validity 10000

storeFile=release-key.jks
storePassword=YOUR_SECURE_PASSWORD_HERE
keyAlias=volleyscore
keyPassword=YOUR_SECURE_PASSWORD_HERE
